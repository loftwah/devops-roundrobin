apiVersion: v1
kind: Service
metadata:
  name: round-robin-worker
  labels:
    app.kubernetes.io/name: round-robin-worker
spec:
  selector:
    app.kubernetes.io/name: round-robin-worker
  ports:
    - name: http
      port: 80
      targetPort: http
